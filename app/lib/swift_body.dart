import 'package:flutter/material.dart';
import 'package:swift/swift_threads.dart';
class SwiftBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
      child: new SwiftThread(),
    );
  }
}